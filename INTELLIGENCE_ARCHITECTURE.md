# Intelligence System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │   Chat   │  │ History  │  │Intelligence│ │ Profile  │       │
│  │   💬     │  │   📜     │  │    🧠     │  │   👤     │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                      FRONTEND LAYER                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  useIntelligence Hook                                     │  │
│  │  • trackBehavior()                                        │  │
│  │  • analyzeConversation()                                  │  │
│  │  • fetchIntelligence()                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              ↕                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Local Storage (Browser)                                  │  │
│  │  • chatty_user_intelligence (Profile Cache)              │  │
│  │  • chatty_behavior_events (Last 100 Events)              │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↕ HTTP/REST
┌─────────────────────────────────────────────────────────────────┐
│                       BACKEND LAYER                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  API Endpoints (intelligence_views.py)                    │  │
│  │  • GET  /api/intelligence/user/                          │  │
│  │  • GET  /api/intelligence/context/                       │  │
│  │  • POST /api/intelligence/analyze/{id}/                  │  │
│  │  • POST /api/intelligence/analyze-all/                   │  │
│  │  • DELETE /api/intelligence/reset/                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              ↕                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Intelligence Service (intelligence_service.py)           │  │
│  │  • analyze_conversation()                                 │  │
│  │  • _extract_question_types()                             │  │
│  │  • _extract_topics()                                     │  │
│  │  • _detect_preferences()                                 │  │
│  │  • _learn_from_insights()                                │  │
│  │  • get_user_profile()                                    │  │
│  │  • get_personalized_context()                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              ↕                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Database Models (intelligence_models.py)                 │  │
│  │  • UserIntelligence                                       │  │
│  │  • ConversationInsight                                    │  │
│  │  • LearningEvent                                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE LAYER                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  SQLite / PostgreSQL                                      │  │
│  │  Tables:                                                  │  │
│  │  • chat_userintelligence                                  │  │
│  │  • chat_conversationinsight                               │  │
│  │  • chat_learningevent                                     │  │
│  │  • chat_conversation                                      │  │
│  │  • chat_message                                           │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagram

### Message Send Flow with Intelligence

```
User types message
       ↓
[ChatInterface]
       ↓
trackBehavior('message_sent', {...})
       ↓
[Local Storage] ← Store behavior event
       ↓
api.sendMessage(conversationId, content, provider, model, userId)
       ↓
[Backend API] /messages/send/
       ↓
IntelligenceService.get_personalized_context(userId)
       ↓
[Database] Query UserIntelligence
       ↓
Build context string: "User prefers detailed responses..."
       ↓
Add to AI prompt: system_message + personalized_context
       ↓
[AI Service] Generate response
       ↓
Save messages to database
       ↓
Return response to frontend
       ↓
[ChatInterface] Display messages
       ↓
Check: messages.length % 3 === 0?
       ↓ (Yes)
analyzeConversation(conversationId)
       ↓
[Backend API] /intelligence/analyze/{id}/
       ↓
[IntelligenceService] analyze_conversation()
       ↓
Extract patterns, preferences, topics
       ↓
[Database] Update UserIntelligence, ConversationInsight, LearningEvent
       ↓
Return insights
       ↓
[Frontend] Update local storage cache
```

## Intelligence Learning Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    CONVERSATION                             │
│  User: "How do I use React hooks? Show me examples."       │
│  AI: "Here's a step-by-step guide with code..."            │
│  User: "Can you explain useEffect too?"                    │
│  AI: "useEffect is a hook that..."                         │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                 PATTERN EXTRACTION                          │
│  • Question types: ["how_to", "explain"]                   │
│  • Topics: ["react", "hooks"]                              │
│  • Message length: avg 45 chars                            │
│  • Contains "show me examples" → prefers_code_examples     │
│  • Contains "step-by-step" → prefers_step_by_step          │
│  • Follow-up question → follow_up_count++                  │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                 INTELLIGENCE UPDATE                         │
│  UserIntelligence:                                          │
│    • category: "preference"                                 │
│    • key: "code_examples"                                   │
│    • value: true                                            │
│    • confidence: 0.6 → 0.7 (increased)                     │
│                                                             │
│  ConversationInsight:                                       │
│    • conversation_id: 123                                   │
│    • prefers_code_examples: true                           │
│    • topics_discussed: ["react", "hooks"]                  │
│                                                             │
│  LearningEvent:                                             │
│    • event_type: "preference_learned"                      │
│    • description: "Learned code_examples from conv 123"    │
│    • confidence: 0.7                                        │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│              PERSONALIZED CONTEXT                           │
│  "User prefers code examples, step by step.                │
│   User is interested in: react, hooks."                    │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                 NEXT AI RESPONSE                            │
│  System: "You are a helpful AI assistant.                  │
│           User context: User prefers code examples..."      │
│                                                             │
│  → AI generates response with code examples                │
│  → AI uses step-by-step format                             │
│  → AI focuses on React topics                              │
└─────────────────────────────────────────────────────────────┘
```

## Component Interaction Diagram

```
┌──────────────────┐
│   app/page.tsx   │
│  Main App Shell  │
└────────┬─────────┘
         │
         ├─────────────────────────────────────────┐
         │                                         │
         ↓                                         ↓
┌──────────────────┐                    ┌──────────────────┐
│ ChatInterface    │                    │IntelligenceProfile│
│                  │                    │                  │
│ • Send messages  │                    │ • View stats     │
│ • Track behavior │                    │ • View prefs     │
│ • Auto analyze   │                    │ • Analyze all    │
└────────┬─────────┘                    │ • Reset          │
         │                              └────────┬─────────┘
         │                                       │
         └───────────────┬───────────────────────┘
                         │
                         ↓
                ┌──────────────────┐
                │ useIntelligence  │
                │      Hook        │
                │                  │
                │ • State mgmt     │
                │ • API calls      │
                │ • Local storage  │
                │ • Behavior track │
                └────────┬─────────┘
                         │
                         ↓
                ┌──────────────────┐
                │   lib/api.ts     │
                │  API Client      │
                │                  │
                │ • HTTP requests  │
                │ • Error handling │
                └────────┬─────────┘
                         │
                         ↓
                ┌──────────────────┐
                │  Backend APIs    │
                └──────────────────┘
```

## Database Schema

```
┌─────────────────────────────────────────────────────────────┐
│                    UserIntelligence                         │
├─────────────────────────────────────────────────────────────┤
│ id (PK)                                                     │
│ user_id (VARCHAR, INDEX)                                    │
│ category (VARCHAR, INDEX) [preference|pattern|topic|...]    │
│ key (VARCHAR)                                               │
│ value (JSON)                                                │
│ confidence (FLOAT)                                          │
│ learned_at (DATETIME)                                       │
│ updated_at (DATETIME, INDEX)                                │
│ source_conversations (JSON)                                 │
│ metadata (JSON)                                             │
│                                                             │
│ UNIQUE(user_id, category, key)                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                 ConversationInsight                         │
├─────────────────────────────────────────────────────────────┤
│ id (PK)                                                     │
│ conversation_id (INT, UNIQUE, INDEX)                        │
│ user_id (VARCHAR, INDEX)                                    │
│ avg_message_length (INT)                                    │
│ response_time_pattern (JSON)                                │
│ question_types (JSON)                                       │
│ topics_discussed (JSON)                                     │
│ sentiment_pattern (JSON)                                    │
│ conversation_length (INT)                                   │
│ session_duration (FLOAT)                                    │
│ follow_up_questions (INT)                                   │
│ clarification_requests (INT)                                │
│ prefers_detailed_responses (BOOL)                           │
│ prefers_code_examples (BOOL)                                │
│ prefers_step_by_step (BOOL)                                 │
│ created_at (DATETIME)                                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    LearningEvent                            │
├─────────────────────────────────────────────────────────────┤
│ id (PK)                                                     │
│ user_id (VARCHAR, INDEX)                                    │
│ event_type (VARCHAR, INDEX)                                 │
│ description (TEXT)                                          │
│ data (JSON)                                                 │
│ confidence (FLOAT)                                          │
│ timestamp (DATETIME, INDEX)                                 │
└─────────────────────────────────────────────────────────────┘
```

## State Management

```
┌─────────────────────────────────────────────────────────────┐
│                    FRONTEND STATE                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  React State (useIntelligence hook):                        │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ intelligence: UserIntelligence | null                 │ │
│  │ stats: IntelligenceStats | null                       │ │
│  │ loading: boolean                                      │ │
│  │ error: string | null                                  │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  Local Storage:                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ chatty_user_intelligence: {                           │ │
│  │   user_id: string,                                    │ │
│  │   preferences: {...},                                 │ │
│  │   topics: {...},                                      │ │
│  │   styles: {...},                                      │ │
│  │   last_updated: string                                │ │
│  │ }                                                     │ │
│  │                                                       │ │
│  │ chatty_behavior_events: [                            │ │
│  │   { type, data, timestamp },                         │ │
│  │   ...                                                │ │
│  │ ]                                                    │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Security Model

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. User Isolation                                          │
│     • Each user_id has separate intelligence                │
│     • No cross-user data access                             │
│     • Queries filtered by user_id                           │
│                                                             │
│  2. Data Privacy                                            │
│     • No PII stored                                         │
│     • Pattern-based learning only                           │
│     • Can be reset anytime                                  │
│                                                             │
│  3. Local Storage                                           │
│     • Browser-encrypted                                     │
│     • Domain-isolated                                       │
│     • User-controlled                                       │
│                                                             │
│  4. Database                                                │
│     • Django ORM (SQL injection protected)                  │
│     • Indexed queries (performance)                         │
│     • Transactional updates                                 │
│                                                             │
│  5. API                                                     │
│     • Input validation                                      │
│     • Error handling                                        │
│     • Rate limiting (future)                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Performance Optimization

```
┌─────────────────────────────────────────────────────────────┐
│                  OPTIMIZATION STRATEGIES                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. Caching                                                 │
│     • Local storage for instant access                      │
│     • Reduces API calls                                     │
│     • Syncs on updates                                      │
│                                                             │
│  2. Async Processing                                        │
│     • Analysis doesn't block UI                             │
│     • Background intelligence updates                       │
│     • Error handling prevents failures                      │
│                                                             │
│  3. Database Indexing                                       │
│     • user_id indexed                                       │
│     • category indexed                                      │
│     • timestamp indexed                                     │
│     • Fast queries                                          │
│                                                             │
│  4. Throttling                                              │
│     • Analyze every N messages (not every message)          │
│     • Batch updates                                         │
│     • Reduces overhead                                      │
│                                                             │
│  5. Data Limits                                             │
│     • Last 100 behavior events                              │
│     • Recent 20 conversations for analysis                  │
│     • Prevents unbounded growth                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

**Architecture Documentation v1.0** | ChattyAI Intelligence System
